// styles/global.js
import { StyleSheet } from "react-native";

const GlobalStyles = StyleSheet.create({
  text: {
    fontFamily: "GoogleSans-Regular", // Default font
    fontSize: 16,
    color: "#fff",
  },
  heading: {
    fontFamily: "GoogleSans-Bold", // Bold font for headings
    fontSize: 20,
    color: "#fff",
  },
});

export default GlobalStyles;
